<Window x:Class="Saga.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Saga.UI.ViewModels"
        mc:Ignorable="d"
        Title="SAGA DYNAMOMETER" Height="720" Width="1080"
        Background="#1E1E1E" WindowStartupLocation="CenterScreen">

    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Panel Izquierdo -->
        <Border Grid.Column="0" Background="#252526" BorderThickness="0,0,1,0" BorderBrush="#333">
            <StackPanel Margin="15">
                <TextBlock Text="PUERTO COM" Foreground="Gray" FontWeight="Bold" Margin="0,0,0,5"/>
                <TextBox Text="{Binding PuertoSeleccionado}" Height="30" FontSize="14" VerticalContentAlignment="Center"/>

                <Button Command="{Binding ConectarCommand}" Content="CONECTAR" Height="40" Margin="0,10,0,0" Background="#007ACC" Foreground="White" FontWeight="Bold"/>
                <Button Command="{Binding DesconectarCommand}" Content="DESCONECTAR" Height="40" Margin="0,10,0,0" Background="#E51400" Foreground="White" FontWeight="Bold"/>

                <Separator Margin="0,20" Background="#333"/>

                <Button Command="{Binding IniciarEnsayoCommand}" Content="▶ INICIAR PRUEBA" Height="60" Background="#107C10" Foreground="White" FontSize="16" FontWeight="Bold"/>
            </StackPanel>
        </Border>

        <!-- Panel Derecho -->
        <Grid Grid.Column="1" Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="30"/>
            </Grid.RowDefinitions>

            <TextBlock Text="MONITOR DE DATOS" Foreground="White" FontSize="20" FontWeight="Light"/>

            <Border Grid.Row="1" Background="Black" Margin="0,10" CornerRadius="5">
                <ScrollViewer>
                    <TextBlock Text="{Binding LogData}" Foreground="#00FF00" FontFamily="Consolas" Margin="10"/>
                </ScrollViewer>
            </Border>

            <TextBlock Grid.Row="2" Text="{Binding MensajeEstado}" Foreground="White" VerticalAlignment="Center"/>
        </Grid>
    </Grid>
</Window>
